;; -------------------------------------------------------------------------------------
;; load zen-burn theme
(add-hook 'after-init-hook (lambda () (load-theme 'zenburn t)))
;; --------------------------------------------------------------------------------------
